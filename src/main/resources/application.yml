spring:
  application:
    name: country-service
server:
  port: ${PORT:8081}
  servlet:
    context-path: /cservice

eureka:
  client:
    serviceUrl:
      defaultZone: https://172.18.133.42/eureka/

